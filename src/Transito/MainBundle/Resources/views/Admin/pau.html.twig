{% extends "TransitoMainBundle:Admin:layout.html.twig" %}
{% use "TransitoMainBundle:Admin:modal.inc.twig" %}
{% set title = 'Puntos de atención' %}
{% set page_title = title %}
{% set page_subtitle = title %}
{% block page_content %}
    <div class="row">
        <div class="col-md-12">
            <!-- start: TABLE WITH IMAGES PANEL -->
            <div class="panel panel-white">
                <div class="panel-body">
                    <table class="table table-striped table-hover" id="sample-table-2">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th>Punto</th>
                                <th>Dirección</th>
                                <th>Email</th>
                                <th>Horario de atención</th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for pau in paus %}
                                <tr>
                                    <td>{{ pau.getId() }}</td>
                                    <td>{{ pau.getName() }}</td>
                                    <td>{{ pau.getLocation() }}</td>
                                    <td>{{ pau.getEmail() }}</td>
                                    <td>{{ pau.getOperation() }}</td>

                                    <td class="center">
                                        <div class="visible-md visible-lg hidden-sm hidden-xs">
                                            <a href="{{ path('user_new_page', {'pau': pau.getID()}) }}" class="btn btn-xs btn-green tooltips" data-placement="top" data-original-title="Registrar usuario"><i class="fa fa-user"></i></a>
                                            <a href="{{ path('pau_doctors_list_page', {'pau': pau.getID()}) }}" class="btn btn-xs btn-yellow tooltips" data-placement="top" data-original-title="Medicos y funcionarios"><i class="fa fa-user-md"></i></a>
                                            <a href="{{ path('pau_edit_page', {'pauID': pau.getId()}) }}" class="btn btn-xs btn-blue tooltips" data-placement="top" data-original-title="Editar"><i class="fa fa-edit"></i></a>
                                        </div>
                                        <div class="visible-xs visible-sm hidden-md hidden-lg">
                                            <div class="btn-group">
                                                <a class="btn btn-green dropdown-toggle btn-sm" data-toggle="dropdown" href="#">
                                                    <i class="fa fa-cog"></i> <span class="caret"></span>
                                                </a>
                                                <ul role="menu" class="dropdown-menu pull-right dropdown-dark">
                                                    <li>
                                                        <a role="menuitem" tabindex="-1" href="{{ path('user_new_page', {'pau': pau.getID()}) }}">
                                                            <i class="fa fa-user"></i> Registrar usuario
                                                        </a>
                                                    </li>
                                                    <li>
                                                        <a role="menuitem" tabindex="-1" href="{{ path('pau_doctors_list_page', {'pau': pau.getID()}) }}">
                                                            <i class="fa fa-user-md"></i> Medicos y funcionarios
                                                        </a>
                                                    </li>
                                                    <li>
                                                        <a role="menuitem" tabindex="-1" href="{{ path('pau_edit_page', {'pauID': pau.getId()}) }}">
                                                            <i class="fa fa-edit"></i> Editar
                                                        </a>
                                                    </li>
                                                </ul>
                                            </div>
                                        </div></td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <!-- end: TABLE WITH IMAGES PANEL -->

            <a class="btn btn-green btn-lg btn-block" href="{{ path('pau_new_page') }}">
                Agregar nuevo punto de atención <i class="fa fa-arrow-circle-right"></i>
            </a>
            <br />
        </div>
    </div>
{% endblock %}